@model IEnumerable<websitedienlanh.Models.DanhMuc>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LoaiDanhMucID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TenDanhMuc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LoaiDanhMucID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TenDanhMuc)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DanhMucID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DanhMucID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DanhMucID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
